import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Header } from "@/components/layout/header";
import { Footer } from "@/components/layout/footer";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "VotreNom.dev - Data Engineer & Expert MLOps",
  description: "Expert en Data Engineering et MLOps, je transforme vos données en avantage concurrentiel grâce à des architectures modernes et des pratiques DevOps éprouvées.",
  keywords: ["Data Engineering", "MLOps", "Cloud Architecture", "Data Governance", "DevOps"],
  authors: [{ name: "Votre Nom" }],
  creator: "Votre Nom",
  publisher: "Votre Nom",
  formatDetection: {
    email: false,
    address: false,
    telephone: false,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="fr" className="scroll-smooth">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased min-h-screen flex flex-col`}
      >
        <Header />
        <main className="flex-grow pt-16">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
}
